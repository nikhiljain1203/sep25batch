package CaseStudy.tictactoe.strategies;

import CaseStudy.tictactoe.models.Board;
import CaseStudy.tictactoe.models.Move;
import CaseStudy.tictactoe.models.Player;

public class MediumBotStrategy implements BotStrategy {
    @Override
    public Move makeMove(Board board, Player player) {
        // Medium bot strategy implementation
        // For simplicity, let's assume it makes a random valid move
        return null; // No valid moves left
    }
}
